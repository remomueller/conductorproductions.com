%h1 Projects

- if current_user.system_admin?
  = link_to 'New Project', new_project_path, class: 'btn btn-xs btn-default'

%table.table.table-striped
  %thead
    %tr
      %th Title
      %th URL Slug
      %th Agency Logo
      %th Owner
      %th{ colspan: "2" }

  %tbody
    - @projects.each do |project|
      %tr
        %td= link_to project.name, project
        %td= project.slug
        %td= project.agency_logo_identifier
        %td= project.user.name if project.user
        %td= link_to 'Edit', edit_project_path(project)
        %td= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } if project.deletable_by?(current_user)
