%ul.project-menu
  %li.active
    CREATIVE
    %ul
      - @project.categories.each do |category|
        %li= link_to category.name, client_project_category_path(@project, category)
  %li
    = link_to 'TIMELINE', client_project_timeline_path
  %li
    PRODUCTION
    %ul
      %li= link_to 'Casting', client_project_casting_path
      %li Locations
      %li Production Elements
  %li
    EDITORIAL


- if @category
  %br
  %br
  %h4{ style: 'color:#333' } Archive

  %div{ style: 'color:#333;word-wrap: break-word;' }
    - @category.documents.where(archived: true).each do |document|
      = link_to document.name, client_project_category_document_path(@project, @category, document)
